String myString = new String("hello");    // Sets myString to a new String object instance that contains "hello".

String myString = "hello";                // Special case of String that does the same as the new keyword.

String myString1 = myString;              // myString1 now references myString, that is both
                                          // String variables point to the same instance of a
                                          // String object containing "hello".
