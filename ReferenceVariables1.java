String myString = new String("hello");    // Sets myString to a new String instance that contains "hello".

String myString = "hello";                // Special case of String that does the same as the new keyword.

String myString1 = myString;              // myString1 now references myString.
                                          // Both strings point to the same instance of a
                                          // String containing "hello".
